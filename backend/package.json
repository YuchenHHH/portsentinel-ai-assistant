{
  "name": "backend",
  "version": "1.0.0",
  "description": "PortSentinel AI Assistant Backend - FastAPI服务",
  "private": true,
  "scripts": {
    "dev": "cd app && python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": "cd app && python -m uvicorn main:app --host 0.0.0.0 --port 8000",
    "install-deps": "pip install -r requirements.txt",
    "test": "cd app && python -m pytest tests/ -v",
    "lint": "cd app && python -m flake8 . && python -m black --check . && python -m isort --check-only .",
    "format": "cd app && python -m black . && python -m isort ."
  },
  "engines": {
    "python": ">=3.8.0"
  }
}
